TS_EXTRA_SEARCH_PATHS=(~/work)

# Session commands run in high-index windows (70+) of the current session.
# Invoke with: tmux-sessionizer -s <index>
# Example splits: tmux-sessionizer -s 0 --vsplit
TS_SESSION_COMMANDS=(
    "btop"
    "lazygit"
)

TS_LOG=file
