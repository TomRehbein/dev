- ~~more setup for tmux-sessionizer~~ ✓ DONE
    - `TS_SESSION_COMMANDS=(btop lazygit)` in tmux-sessionizer.conf konfiguriert
    - Aufruf: `tmux-sessionizer -s 0` (btop) / `tmux-sessionizer -s 1` (lazygit)
- default scripts for rm -rf node-modules or other directories and installing and starting server
    - maybe also for other languages

